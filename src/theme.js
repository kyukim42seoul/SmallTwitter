// index.css 로 라이트모드 작업 후 다크모드 적용할 때 이 파일 작성

const system = {
  info,
  success,
  warning,
  danger,
  dangerLighten,
  dangerDarken,
  dangerDisabled,
};

const greyScale = {
  white,
  grey1,
  grey2,
  grey3,
  grey4,
  grey5,
  black,
};

const brand = {
  primary,
  primaryLigten,
  primaryDarken,
  primaryDisabled,
};

const color = {
  system,
  greyScale,
  brand,
};

const fontSize = {};

const theme = {};

export default theme;
